import { ResponseTransformInterceptor } from './response-transform-interceptor.interceptor';

describe('ResponseTransformInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseTransformInterceptor()).toBeDefined();
  });
});
